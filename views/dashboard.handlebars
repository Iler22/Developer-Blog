{{> privateNavbar}}
<main>
<div class='jumbotron text-center'>
  <h1 class='display-4'>Welcome {{user.username}}</h1>
  <hr class='my-4' />
  <p class='lead'>Here is a list of your blogs</p>
  <a class="btn btn-success" href="/create-blog" role="button">Add Blog</a>
</div>
  <div class="d-flex flex-column bd-highlight mb-3 justify-content-center">
      {{#each blogs as | blog |}}
        {{> blogCard}}
      {{/each}}  
  </div>
</main>