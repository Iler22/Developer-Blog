{{#if loggedIn}}
    {{> privateNavbar}}
{{else}}
    {{> publicNavbar}}
{{/if}}
<main>
  {{> banner}}
  <div class="d-flex flex-column bd-highlight mb-3 justify-content-center">
      {{#each blogs as | blog |}}
        {{> blogCard}}
      {{/each}}  
  </div>
</main>
